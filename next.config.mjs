/** @type {import('next').NextConfig} */
const nextConfig = {
  // Only the /api routes are used — the Vite frontend handles the UI.
  // Next.js experimental features can be added here as needed.
};

export default nextConfig;
